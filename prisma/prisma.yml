#The HTTP Endpoint for your API
#endpoint: ''
endpoint: https://eu1.prisma.sh/somethingscripted/hackernews-node/dev

# Points to the file that contains your datamodel
datamodel: datamodel.prisma

# Specifies language & localization for the generated Prisma client
generate:
  - generator: javascript-client
    output: ../src/generated/prisma.client
